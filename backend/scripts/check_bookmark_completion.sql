-- Replace these values with the actual user_id and note_id you want to check
SELECT * FROM bookmarks WHERE user_id = '<USER_ID>' AND note_id = '<NOTE_ID>';
SELECT * FROM completions WHERE user_id = '<USER_ID>' AND note_id = '<NOTE_ID>';
