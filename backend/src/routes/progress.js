import express from "express";

const router = express.Router();

// Legacy progress & analytics routes intentionally removed.
// This stub exists only to satisfy imports and prevent regressions.

export default router;
